{{>navbar }}

<div class="container videos">
    {{!-- test to see if no posts --}} {{#each videos}}
    <div class="video">   
        <div class="video-container">
            <iframe width="640" height="360" src="{{url}}" frameborder="0" allow="autoplay; encrypted-media"
                allowfullscreen></iframe>
        </div>
    </div>
    {{/each}}
</div>